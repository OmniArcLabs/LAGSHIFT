# LAGSHIFT 1.3.0 — FusionDNS Hybrid Update

تاریخ انتشار: ۲۰۲۶-۰۹-۱۵

## FusionDNS Hybrid

- DNSهای ایرانی و خارجی با تست زندهٔ همان دستگاه مقایسه می‌شوند.
- موتور به‌جای تکیه بر یک پینگ کلی، تک‌تک مقصدهای لازم را بررسی می‌کند.
- در بخش برنامه‌ها، مقصدهای Login، API و CDN هر پروفایل جداگانه سنجیده می‌شوند.
- DNS دارای پوشش کامل، از گزینهٔ سریع‌تری که بخشی از مقصدها را باز نمی‌کند بالاتر قرار می‌گیرد.
- حالت «انتخاب خودکار» در تب DNS اکنون از موتور FusionDNS استفاده می‌کند.

## اتصال برنامه‌ها

- حداکثر چهار DNS برتر ایرانی و خارجی در یک مسابقهٔ زمان‌دار تأیید می‌شوند.
- در ویندوز، تمام نامزدها داخل یک درخواست Administrator بررسی می‌شوند.
- نتیجهٔ انتخاب، درصد پوشش و مراحل بررسی به زبان ساده نمایش داده می‌شوند.
- اگر هیچ مسیر سالمی پیدا نشود، DNS قبلی به‌صورت خودکار بازگردانده می‌شود.
- WARP رسمی همچنان فقط پس از شکست DNS و با تأیید واقعی مسیر آزمایش می‌شود.

## RouteDNA و صداقت نتیجه

- حافظهٔ محلی RouteDNA دیگر نمی‌تواند یک DNS قدیمی اما ناقص را بالاتر از گزینه‌ای با پوشش کامل قرار دهد.
- سابقه فقط نقش کمکی دارد و نتیجهٔ زندهٔ همین شبکه اولویت اصلی است.
- FusionDNS در این نسخه «انتخاب ترکیبی» است: DNSهای دو خانواده را مقایسه می‌کند و یک پروفایل سالم را اعمال می‌کند.
- این نسخه ادعای Split-DNS هم‌زمان، DNS Proxy محلی یا Relay اختصاصی ندارد.

## امنیت و نصب

- مانیفست آپدیت با Ed25519 و فایل نصب با SHA-256 و اندازهٔ مورد انتظار کنترل می‌شود.
- نصب‌کننده پس از ارتقا، سلامت Runtime و اجزای Qt/Python را آزمایش می‌کند و در صورت خرابی به نسخهٔ قبلی برمی‌گردد.
- نسخه همچنان گواهی تجاری Authenticode ندارد؛ فایل را فقط از Release رسمی OMNIARC دریافت و SHA-256 آن را بررسی کنید.

## وضعیت تأیید

- ۱۷۹ تست خودکار ویندوز با موفقیت اجرا شده‌اند.
- CI ویندوز و تحلیل امنیتی CodeQL روی کد انتشار موفق بوده‌اند.
- موفقیت واقعی هر DNS به اپراتور، زمان و شبکهٔ کاربر وابسته است و برنامه نتیجه را روی همان دستگاه تأیید می‌کند.
