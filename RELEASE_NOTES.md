# LAGSHIFT 1.0.4 local-first Stable — Release Notes

تاریخ Build مرجع: ۲۰۲۶-۰۹-۰۹

## آپدیت امن درون‌برنامه‌ای

- رفع قطعی گیرکردن اعلان‌های بالای برنامه با زمان انقضای مستقل و محدود
- جلوگیری از تمدید یا نمایش دوبارهٔ پیام‌های تکراری مانیتورهای پس‌زمینه
- زمان نمایش هوشمند براساس نوع و طول پیام، همراه با بستن دستی و سقف ۱۲ ثانیه
- جداسازی افکت اعلان از انیمیشن شروع برنامه و بازیابی خودکار افکت در صورت جایگزینی
- ترمیم ارتقای نسخه: جایگزینی یک‌پارچهٔ مجموعه Qt/Python پیش از اولین اجرا
- بازگشت به محیط تثبیت‌شده Python 3.12 و PySide6 6.8.3
- بررسی کامل‌بودن فایل‌های اجرایی پس از نصب و بازگردانی تمیز در صورت شکست
- پایان قطعی و قابل‌فهم برای بررسی RouteDNA در حالت موفق، خطا و توقف زمانی
- حذف پنل قدیمی مسیرهای تانل از نسخه عمومی و توضیح دقیق رفتار WARP رسمی
- حذف چشمک پنجره PowerShell هنگام بررسی امضای کلاینت Cloudflare

- بررسی آرام کانال Stable یا Beta در پس‌زمینه هنگام شروع برنامه
- نمایش نسخه، توضیحات انتشار و مسیر دانلود در پنجرهٔ اختصاصی LAGSHIFT
- دانلود قابل توقف و ادامه بدون دورریختن بخش سالم فایل
- تأیید مانیفست با Ed25519 و کنترل اندازه و SHA-256 فایل، هم پس از دانلود و هم
  درست پیش از اجرای نصب‌کننده
- توقف آپدیت هنگام اجرای بازی و خروج امن برنامه پس از تحویل به نصب‌کننده
- اعلام شفاف نبود Authenticode و نیاز به پذیرش جداگانه پیش از اجرای نسخهٔ بدون
  امضای تجاری ویندوز

## قابلیت‌های اصلی

- Smart DNS با تست چندنمونه‌ای، انتخاب سرعت/پایداری/تعادل و تعویض امن
- AppRoute برای بیست برنامه و لانچر با بررسی DNS، TCP، TLS، Login و CDN
- RouteDNA با حافظه محلی هش‌شده، تشخیص تغییر شبکه، Data Budget و توضیح مسیر
- تشخیص بازی، Lobby/Match تخمینی، Route Profile، Route Lab، MTU و QoS محدود
- WARP Bridge رسمی با تأیید امضای Cloudflare و گزارش صادقانه شکست
- گزارش خطای محلی پاک‌سازی‌شده و آپدیتر Fail-closed
- Installer سبک و Self-contained با حفظ داده‌های کاربر در ارتقا و حذف

## وضعیت تأیید

- ۱۴۴ تست برنامه، امنیت آپدیت و قرارداد Cloudflare Worker پاس شده‌اند.
- نصب ایزوله، اجرای بسته‌بندی‌شده و حذف تمیز روی میزبان فعلی پاس شده است.
- تست DPI میزبان فعلی در ۱۰۰، ۱۲۵، ۱۵۰ و ۲۰۰ درصد پاس شده است.
- ماتریس زندهٔ خصوصی ۲۰ پروفایل روی شبکهٔ فعلی، ۲۰ از ۲۰ مسیر DNS/TCP/TLS
  سالم گزارش کرد؛ این نتیجه تضمین ورود یا عملکرد یکسان روی اپراتورهای دیگر نیست.

## محدودیت‌های شناخته‌شده

- فایل‌ها Authenticode امضا نشده‌اند و Windows ممکن است Unknown publisher یا
  SmartScreen نمایش دهد. دانلود فقط از صفحهٔ رسمی و تطبیق SHA-256 توصیه می‌شود.
- WARP روی چند ISP و سیستم تأیید مثبت نشده است.
- Relay تولیدی GameLink و Backend عمومی فعال نیستند.
- GameLink، Radar ناشناس، موقعیت جغرافیایی راه دور و تست پهنای باند Backend در
  نسخه عمومی 1.0.4 از سطح هسته غیرفعال‌اند؛ تنظیمات باقی‌مانده از Build آزمایشی نیز
  نمی‌تواند آن‌ها را دوباره فعال کند.
- QA ویندوز/سخت‌افزار/آنتی‌ویروس و Fullscreen چندسیستمی باقی مانده است.

این نسخه Stable محلی است؛ موارد بالا محدودیت‌های شفاف انتشار هستند و قابلیت
زیرساختیِ آزمایشی یا تأییدنشده در آن فعال نشده است.
