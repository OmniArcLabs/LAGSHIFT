# LAGSHIFT 1.2.0 — Windows Reliability Update

تاریخ Build مرجع: ۲۰۲۶-۰۹-۱۳

## آپدیت امن و بازیابی خودکار

- نصب‌کننده پس از کپی فایل‌ها، خود نسخهٔ نصب‌شده و اجزای Qt/Python را اجرا و آزمایش می‌کند.
- اگر Runtime ناقص یا ناسازگار باشد، نصب موفق اعلام نمی‌شود و نسخهٔ قبلی از Rollback محلی برمی‌گردد.
- بررسی امضای Windows/PowerShell در پس‌زمینه و بدون بازشدن پنجرهٔ ناگهانی انجام می‌شود.
- مانیفست، اندازه و SHA-256 فایل همچنان با امضای Ed25519 کنترل می‌شوند.

## اعلان‌های قابل‌اعتماد

- همهٔ پیام‌های بالای برنامه عمر محدود دارند و پس از پایان زمان واقعاً از Layout حذف می‌شوند.
- یک Watchdog مستقل، اعلان را حتی در صورت خرابی Animation یا Graphics Effect جمع می‌کند.
- پیام‌های تکراری مانیتورهای پس‌زمینه نمی‌توانند زمان اعلان را بی‌نهایت تمدید کنند.
- با کلیک روی اعلان می‌توان آن را زودتر بست.

## AppRoute سریع‌تر و آرام‌تر

- آزمایش برنامه‌ها به سه DNS برتر محدود شده و در حالت عادی فقط یک درخواست Administrator دارد.
- مرحلهٔ WARP سقف تلاش و زمان مشخص دارد و لغو کاربر بلافاصله به موتور منتقل می‌شود.
- فرمان‌های RouteDNA، DNS، WARP، Ping، MTU و Packet Monitor بدون پنجره یا چشمک Taskbar اجرا می‌شوند.
- شکست Discord یا اپ‌های مشابه دیگر نباید کاربر را چند دقیقه در حلقهٔ تکرار نگه دارد.
- اگر شبکه همهٔ مقصدهای عمومی را به یک IP خصوصی بسته هدایت کند، AppRoute آن را
  به‌عنوان رهگیری DNS تشخیص می‌دهد، تعویض بی‌اثر DNS و پنجرهٔ Administrator را رد
  می‌کند و مستقیماً سه حالت متفاوت کلاینت رسمی Cloudflare را کوتاه آزمایش می‌کند.
- سنجش TLS به همان IP حل‌شده متصل می‌شود و SNI دامنه را حفظ می‌کند؛ بنابراین نتیجه
  با Resolve دوباره و انتخاب ناخواستهٔ IP دیگری مخدوش نمی‌شود.

## تشخیص تعرفهٔ داخلی

- موتور بدون LinkIrani هم با فهرست آفلاین ۲۳۱۲ محدودهٔ IPv4/IPv6 ایران از آمار
  روزانهٔ RIPE NCC نتیجهٔ «احتمالاً داخلی»، «احتمالاً تمام‌بها» یا «ترکیبی» می‌دهد.
- تخصیص IP به ایران فقط نشانهٔ فنی موقعیت است و هیچ‌گاه به‌عنوان تأیید قطعی تعرفه نمایش داده نمی‌شود.
- اپراتور می‌تواند دستی انتخاب شود یا پس از رضایت جداگانه از Cloudflare به‌صورت خودکار تشخیص داده شود.
- LAGSHIFT فیلد IP پاسخ Cloudflare را دور می‌اندازد و آن را نمایش، ذخیره یا گزارش نمی‌کند.
- LinkIrani فقط پس از رضایت جداگانه و به‌عنوان منبع کمکی استفاده می‌شود؛ تنها نام میزبان نهایی بدون مسیر، query یا token ارسال می‌شود.
- پاسخ LinkIrani همیشه «احتمالی و غیرتضمینی» است و جای استعلام رسمی اپراتور را نمی‌گیرد.
- روند بررسی به زبان ساده نمایش داده می‌شود و جزئیات DNS، Redirect، IP و HTTP پشت دکمهٔ جزئیات فنی قرار دارند.

## وضعیت تأیید

- تست‌های خودکار برنامه، امنیت، حریم خصوصی و مسیرهای خطا پاس شده‌اند.
- Health Check برنامه و کامپایل نصب‌کنندهٔ Windows بدون خطا و هشدار تأیید شده‌اند.
- تشخیص اپراتور Cloudflare و استعلام فقط-میزبان LinkIrani با تست زنده بررسی شده‌اند.
- Build عمومی همچنان فاقد موتورهای تانل دلخواه و Subscription است.

## محدودیت‌های شفاف

- تشخیص تعرفه یک راهنمای فنی است؛ ضریب نهایی کسر حجم را قرارداد و اپراتور تعیین می‌کند.
- فایل‌ها گواهی تجاری Authenticode ندارند و Windows ممکن است Unknown publisher یا SmartScreen نشان دهد؛ دانلود فقط از Release رسمی و تطبیق SHA-256 توصیه می‌شود.
- تست میدانی روی چند دستگاه Windows 10/11 و چند اپراتور، پیش از علامت‌زدن نسخه به‌عنوان Stable عمومی لازم است.
- GameLink، Relay عمومی، Radar ناشناس و Backend عمومی در نسخه Stable فعال نیستند.

این نسخه قابلیت زیرساختی تأییدنشده را «موفق» اعلام نمی‌کند و هر تغییر شبکه را با مسیر بازگشت نگه می‌دارد.
