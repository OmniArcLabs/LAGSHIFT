# LAGSHIFT 1.1.0 local-first Stable — Release Notes

تاریخ Build مرجع: ۲۰۲۶-۰۹-۱۱

## تشخیص تعرفهٔ داخلی

- بررسی آدرس سایت یا فایل، تمام تغییرمسیرها و میزبان نهایی دانلود
- نمایش IPv4/IPv6، وضعیت HTTP، اندازهٔ اعلام‌شده و مدرک هر مرحله
- تشخیص محافظه‌کار «ثبت‌شده»، «مسیر ترکیبی» یا «تأییدنشده» بدون تضمین صورتحساب
- اهمیت بیشتر سرور تحویل‌دهندهٔ فایل نسبت به صفحه‌ها و Redirectهای کم‌حجم
- هشدار روشن برای WARP، VPN و بخش‌های ناامن HTTP
- رسید پاک‌سازی‌شده و قابل کپی همراه با اپراتور انتخابی و زمان بررسی
- دسترسی مستقیم به استعلام رسمی سازمان فناوری اطلاعات و مسیر پیگیری ۱۹۵

هیچ لینک یا توکن کاربر بدون رضایت به LinkIrani یا سرویس ثالث دیگری فرستاده
نمی‌شود. query موردنیاز لینک امضاشده فقط هنگام همان بررسی در حافظه استفاده می‌شود
و در نتیجه، تاریخچه یا رسید قرار نمی‌گیرد.

## تجربه و بازیابی

- راه‌اندازی اولیهٔ کوتاه برای انتخاب هدف و توضیح رضایت آگاهانهٔ RouteDNA
- پنل ابزارها و سلامت یکپارچه، کنترل‌های System Tray و وضعیت شفاف‌تر برنامه
- ترمیم نصب از بستهٔ همان نسخه و کانال امضاشده، همراه با مسیر Recovery
- ساخت گزارش پاک‌سازی‌شده و انتقال اختیاری آن به فرم رسمی GitHub Issues

## امنیت و صحت

- کاتالوگ تعرفه فقط با امضای Ed25519 پذیرفته می‌شود و تاریخ انقضا دارد
- مقصدهای محلی و خصوصی در زنجیرهٔ Redirect مسدود می‌شوند
- نبود منبع رسمی پایدار با نتیجهٔ «تأییدنشده» نمایش داده می‌شود، نه حدس از روی
  پسوند `.ir` یا موقعیت تقریبی IP
- آپدیتر همچنان مانیفست، اندازه، SHA-256 و امضای Ed25519 را پیش از اجرا کنترل می‌کند

## وضعیت تأیید

- ۱۶۲ تست برنامه، امنیت و شرایط خصمانه پاس شده‌اند.
- ۹۶ کنترل صفحه‌کلید و رابط در آزمون Accessibility پاس شده‌اند.
- Build عمومی فاقد موتورهای تانل و Subscription غیرفعال است.

## محدودیت‌های شناخته‌شده

- سازمان فناوری اطلاعات API عمومی بدون کپچا برای دریافت فهرست کامل ارائه نمی‌کند؛
  بنابراین کاتالوگ داخلی برنامه تا دسترسی به منبع مجاز و قابل حسابرسی خالی است.
- ثبت در فهرست تعرفهٔ داخلی، مقدار دقیق کسر بسته توسط اپراتور را تضمین نمی‌کند.
- فایل‌ها گواهی تجاری Authenticode ندارند و Windows ممکن است Unknown publisher
  یا SmartScreen نمایش دهد؛ دانلود فقط از Release رسمی و تطبیق SHA-256 توصیه می‌شود.
- آزمون مستقل چندسیستمی و چنداپراتوری همچنان یک محدودیت شفاف انتشار است.
- GameLink، Relay عمومی، Radar ناشناس و Backend عمومی در نسخه Stable فعال نیستند.

این نسخه یک محصول local-first است و قابلیت زیرساختی تأییدنشده را به‌صورت نمایشی
«موفق» اعلام نمی‌کند.
