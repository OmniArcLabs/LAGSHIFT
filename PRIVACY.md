# سیاست حریم خصوصی LAGSHIFT

نسخه: ۱.۳ — تاریخ بازبینی: ۲۰۲۶-۰۹-۰۸

LAGSHIFT برای بررسی و بهینه‌سازی اتصال بازی‌ها و برنامه‌های پشتیبانی‌شده طراحی
شده است. اصل محصول «پردازش محلی، حداقل داده و رضایت جداگانه» است.

## اطلاعاتی که به‌صورت محلی پردازش می‌شوند

- نام کارت شبکه و وضعیت IPv4/IPv6 برای اجرای عملیات روی همان دستگاه
- پینگ، جیتر، قطعی، DNS، TLS، MTU و فشار تقریبی شبکه
- نام پردازش بازی یا برنامه انتخاب‌شده برای تشخیص و QoS محدود
- نتایج RouteDNA با شناسه یک‌طرفه و هش‌شده شبکه
- گزارش خطای پاک‌سازی‌شده، در صورت روشن‌بودن جعبه‌سیاه محلی

این اطلاعات به‌طور پیش‌فرض از دستگاه خارج نمی‌شوند. محتوای پیام، رمز عبور،
توکن ورود، محتوای TLS، تاریخچه مرورگر و فایل‌های شخصی خوانده نمی‌شوند.

## قابلیت‌های اختیاری شبکه

اطلاعات تقریبی کشور، منطقه، ASN و نام اپراتور فقط پس از رضایت جداگانه و تنها
زمانی دریافت می‌شوند که Backend رسمی HTTPS پیکربندی شده باشد. سرور اینترنتی
طبق ماهیت شبکه IP درخواست را می‌بیند، اما برنامه IP عمومی را در تاریخچه نمایش
یا ذخیره نمی‌کند. یادگیری جمعی و ارسال گزارش خطا نیز به‌صورت پیش‌فرض خاموش‌اند.

## سرویس‌های ثالث

WARP Bridge فقط کلاینت رسمی Cloudflare را کنترل می‌کند. در صورت فعال‌سازی،
پردازش داده تابع شرایط و سیاست Cloudflare است و ممکن است ترافیک کل دستگاه از
شبکه آن عبور کند. LAGSHIFT فایل اجرایی Cloudflare را بازتوزیع نمی‌کند.

## نگه‌داری و حذف

تنظیمات و تاریخچه محلی تا زمان حذف توسط کاربر یا پاک‌سازی برنامه باقی می‌مانند.
حذف برنامه نباید بدون انتخاب روشن کاربر داده شخصی را پاک کند. برای Backend
تولیدی باید پیش از فعال‌سازی، مدت نگه‌داری، روش حذف و نشانی تماس ناشر مشخص شود.

## گزارش مشکل

کاربر باید قبل از ارسال، پیش‌نمایش گزارش را ببیند. گزارش مجاز نیست شامل IP
عمومی، نام کاربری، Token، Secret، لینک کانفیگ، فهرست پردازش‌ها یا دامنه شخصی
باشد. تا زمان پیکربندی Backend رسمی، گزارش‌ها فقط به‌صورت فایل محلی صادر می‌شوند.

## تماس

پرسش‌های حریم خصوصی و درخواست‌های پشتیبانی از طریق بخش Issues مخزن رسمی دریافت
می‌شوند: https://github.com/OmniArcLabs/LAGSHIFT/issues . اطلاعات حساس، گزارش خام
یا تصویر دارای داده شخصی نباید در Issue عمومی قرار گیرد؛ موارد امنیتی فقط از مسیر
Private Vulnerability Reporting مخزن ارسال شوند.
