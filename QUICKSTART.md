# شروع سریع LAGSHIFT

1. برنامه را نصب و در اجرای اول شرایط استفاده و حریم خصوصی را مطالعه کن.
2. از تب DNS، کارت شبکه را انتخاب و «اتصال هوشمند» را اجرا کن.
3. هدف را متناسب با نیازت روی بازی رقابتی، پایداری، دانلود یا رفع محدودیت
   برنامه قرار بده.
4. در «برنامه‌ها»، سرویس موردنظر را انتخاب و آزمایش را اجرا کن. سبزشدن DNS
   به‌تنهایی تضمین ورود نیست؛ مقصدهای DNS، TCP و TLS جداگانه تأیید می‌شوند.
5. در «بازی‌های من»، بازی را اجرا یا انتخاب کن. نتیجه «مقصد مشاهده‌شده» از
   ترافیک همان دستگاه است و لزوماً سرور نهایی بازی نیست.
6. WARP اختیاری و آزمایشی است. فقط کلاینت رسمی Cloudflare را نصب کن و هشدار
   Device-wide بودن مسیر را قبل از ادامه بخوان.
7. اگر اتصال بدتر شد، از «قطع و بازگردانی» استفاده کن. برنامه تلاش می‌کند DNS
   قبلی را دقیقاً بازیابی کند.
8. هنگام بروز خطا، از تنظیمات گزارش تشخیصی پاک‌سازی‌شده بساز؛ رمز، Token یا
   لینک خصوصی را به گزارش اضافه نکن.

LAGSHIFT یک VPN عمومی نیست و کاهش قطعی پینگ یا عملکرد یکسان روی همه اپراتورها
را تضمین نمی‌کند. نتیجه تست زنده همان دستگاه از پیشنهادهای تاریخی مهم‌تر است.
